import "./style.css"
import Typed from 'typed.js';
var options = {
  strings: ['Text Gradients' , 'Make your headlines stand out', "Design is intelligence made visible"],
  typeSpeed: 100,
  backSpeed: 100,
  loop: true,
};

var typed = new Typed('#element', options);
